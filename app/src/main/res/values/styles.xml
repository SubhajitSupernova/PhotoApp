<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- App Theme -->
    <style name="Theme.PhotoEditingSoftware" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">#3E00B6</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="colorSecondary">#00517A</item>
        <item name="colorOnSecondary">#FFFFFF</item>

        <!-- Backgrounds -->
        <item name="android:windowBackground">@drawable/bg_gradient</item>
    </style>

    <!-- Filled Material Button -->
    <style name="Widget.PhotoEditingSoftware.Button.Filled" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">12dp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="backgroundTint">?attr/colorPrimary</item>
    </style>

    <!-- Outlined Material Button -->
    <style name="Widget.PhotoEditingSoftware.Button.Outlined" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="cornerRadius">12dp</item>
        <item name="strokeColor">?attr/colorSecondary</item>
        <item name="android:textColor">?attr/colorSecondary</item>
    </style>

    <!-- TextInput Dropdown -->
    <style name="Widget.PhotoEditingSoftware.TextInputDropdown" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.ExposedDropdownMenu">
        <item name="boxCornerRadiusTopStart">12dp</item>
        <item name="boxCornerRadiusTopEnd">12dp</item>
        <item name="boxCornerRadiusBottomStart">12dp</item>
        <item name="boxCornerRadiusBottomEnd">12dp</item>
    </style>

    <!-- Card Style -->
    <style name="Widget.PhotoEditingSoftware.Card" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">8dp</item>
    </style>

</resources>
